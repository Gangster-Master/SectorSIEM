export enum MonitoringMethods {
    problemGet = 'problem.get',
    problemView = 'problem.view',
}

export enum EventMethods {
    eventGet = 'event.get',
}
export enum HostMethods {
    hostgroupGet = 'hostgroup.get',
}
